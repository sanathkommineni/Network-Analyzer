# Network Analyzer

A lightweight Python network diagnostic tool for Windows.

## Features

- Detects computer hostname
- Detects local IP address
- Detects default gateway
- Checks basic internet connectivity
- Uses Python standard library only

## Run

```bash
py src/analyzer.py